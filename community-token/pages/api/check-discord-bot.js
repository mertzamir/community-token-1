export default function handler(req, res) {
  /* check to see if discord bot was invited */
  // send success or error message based on guild count



  res.status(200).json("Success");

  // res.status(400).json("Bad Request");
}
